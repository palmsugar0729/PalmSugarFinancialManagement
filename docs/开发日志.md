---
date: 2026-06-07
tags: [devlog, flutter, android, palmsugar, setup]
project: palmsugar
aliases: ["2026-06-07 开发日志"]
---

# 2026-06-07 开发日志

> **项目**：[[PalmSugar Financial Management]]
> **关联**：[[PRD]] · [[gradle-mirror-pitfall]] · [[ui-theme-color]]

---

## 一、今日完成

| # | 事项 | 涉及文件 | 备注 |
|---|------|----------|------|
| 1 | 诊断 Gradle 编译网络超时问题 | `android/build.gradle.kts` | `flutter doctor -v` 报 `maven.google.com` 超时 |
| 2 | 配置清华镜像源到 Gradle 仓库和 Wrapper | `android/settings.gradle.kts` | pluginManagement.repositories |
| 3 | 配置清华镜像源到 Gradle Wrapper | `android/gradle/wrapper/gradle-wrapper.properties` | distributionUrl 替换 |
| 5 | 首次编译 Debug APK 成功 | `build/app/outputs/flutter-apk/app-debug.apk` | 耗时约 2h17m，腾讯云 Gradle + 阿里云 Maven 镜像生效 |
| 6 | 替换应用主色调为 `#A3C1AD` | `lib/main.dart` | `ColorScheme.fromSeed(seedColor: Color(0xFFA3C1AD))`；类型色（红/绿/蓝）保持不变 |

---

## 二、遇到的问题

| # | 问题 | 根因 | 修复 |
|---|------|------|------|
| 1 | `flutter build apk --debug` 卡死无响应 | Gradle 首次编译需从海外仓库下载 Android Gradle Plugin、Gradle Wrapper、Kotlin 插件等，国内访问超时 | 将三处仓库配置替换为清华镜像源，详情见 [[gradle-mirror-pitfall]] |
| 2 | Chrome 无法运行 Flutter Web | `sqflite` 和 `path_provider` 没有 Web 平台实现 | 预期内，放弃 Web 预览渠道，专注 Android |

---

## 三、关键决策

| # | 决策点 | 选项A | 选项B | 选择 | 理由 |
|---|--------|-------|-------|------|------|
| 1 | Gradle 镜像源选择 | 清华镜像 | 阿里云镜像 | **阿里云镜像** | 清华未同步 AGP 9.0.1/8.8.0，阿里云 public 仓库覆盖更全 |
| 2 | AGP 版本选择 | 9.0.1（Flutter 模板默认） | 8.8.0（降级） | **8.8.0** | 国内镜像未同步 9.0.1，降级后编译通过；Flutter 报 warning 建议 ≥8.11.1 |

---

## 四、下一步

- [x] 首次编译 Debug APK 成功 —— `app-debug.apk`（~142MB）已生成
- [ ] 真机/模拟器安装测试
- [ ] 根据测试结果调整 UI 细节和交互

---

## 2026-06-08 — 模拟器 & 真机测试

### 完成事项

- [x] 配置 MuMu 模拟器 ADB 连接（端口 7555）
- [x] App 成功运行到 MuMu 模拟器（Android 12）
- [x] 修复「添加分类」Dialog 闪退 bug（`_dependents.isEmpty` 断言失败）
- [x] 优化「添加分类」Hint 文本按类型变化
- [x] 修复「记一笔」保存按钮不明显问题（`TextButton` → `FilledButton`）
- [x] 修复真机安装 Debug APK 闪退（`libflutter.so` arm64-v8a 缺失）
- [x] 重新编译 arm64 架构 APK
- [x] 创建踩坑记录 [`notes/add-category-dialog-crash.md`](../../notes/add-category-dialog-crash.md)、[`notes/flutter-apk-arm64-crash.md`](../../notes/flutter-apk-arm64-crash.md)

### 遇到的问题

| # | 问题 | 根因 | 修复 |
|---|------|------|------|
| 1 | 分类管理「添加分类」闪退 | `TextEditingController` 在 dialog 关闭动画未完成时手动 `dispose`，导致 `_dependents` 未清空 | controller 移入 dialog builder 内部，随 dialog 自动销毁 |
| 2 | 真机安装 APK 闪退 | APK 只包含 `x86_64` 库，真机需要 `arm64-v8a` | `flutter build apk --debug --target-platform=android-arm64` |
| 3 | MuMu 模拟器无法弹出软键盘 | 模拟器限制 | 导出 APK 到真机测试 |

### 下一步

- [x] 真机安装测试 —— arm64 APK 已导出
- [x] 根据真机测试结果调整 UI 细节和交互
- [ ] 第二期开发：账单文件导入

---

## 2026-06-08 — 下午收尾

### 完成事项

- [x] 金额输入框支持简单加减乘除计算器功能
- [x] 修复金额输入框 `InputFormatter` 正则错误（只能输一位数字）
- [x] 修复「记一笔」保存按钮不明显（`TextButton` → `FilledButton`）
- [x] 修复金额输入框按回车/点击空白处不脱离输入状态（FocusScope.unfocus）
- [x] 踩坑记录归档到 `docs/bugs/` 目录
- [x] 对话记录存档

### 遇到的问题

| # | 问题 | 根因 | 修复 |
|---|------|------|------|
| 1 | 金额输入框只能输入单个数字 | `InputFormatter` 正则只匹配单个字符 | `RegExp(r'^[\d+\-*/.]*$')` 改为匹配任意长度 |
| 2 | 按回车/点空白处不收起键盘 | 未调用 `FocusScope.unfocus()` | `onEditingComplete` + `GestureDetector.onTap` 双重处理 |

### 下一步

- [ ] 第二期 MVP：账单文件导入（CSV/Excel）
- [ ] 支付宝/微信账单格式解析

---

## 2026-06-08 — 最终修正

### 完成事项

- [x] 修复最近记录时间显示为 `00:00`（保存时只存了年月日，未存时分秒）
- [x] 反转收入/支出 UI 颜色：收入 → 红色（涨），支出 → 绿色（跌）
- [x] 日期选择器改为中文显示（添加 `flutter_localizations` + `locale: zh_CN`）
- [x] App 改名：`PalmSugar 记账` → `棕榈糖账本`
- [x] 升级 `intl` 到 `^0.20.2` 以兼容 `flutter_localizations`
- [x] 更新 README.md

### 遇到的问题

| # | 问题 | 根因 | 修复 |
|---|------|------|------|
| 1 | 最近记录时间都是 `00:00` | `_saveTransaction` 中 `DateTime` 只传了年月日，时分秒默认 0 | 加入 `DateTime.now()` 的时分秒 |
| 2 | 收入绿/支出红与理财习惯相反 | 默认配色收入=绿、支出=红 | 全部改为收入=红、支出=绿（红涨绿跌） |
| 3 | 日期选择器显示英文 | 未配置中文 locale | `MaterialApp` 添加 `flutter_localizations` + `locale: zh_CN` |
| 4 | `flutter_localizations` 依赖冲突 | `intl ^0.19.0` 与 `flutter_localizations` 要求的 `0.20.2` 冲突 | `intl` 升级到 `^0.20.2` |

### 下一步

---

## 2026-06-09 — 导入导出 Excel + 月份切换 + 分类管理增强

### 完成事项

| # | 事项 | 涉及文件 | 备注 |
|---|------|----------|------|
| 1 | Excel 导入导出功能 | `lib/pages/data_backup_page.dart` | 使用 `excel` + `file_picker` + `share_plus` |
| 2 | 修复导出 share_plus 弹窗 bug | `lib/pages/data_backup_page.dart` | 保存到 Downloads + SnackBar 分享 Action |
| 3 | 修复 Sheet1 空白页 | `lib/pages/data_backup_page.dart` | `excel.rename()` 替代 `delete()` |
| 4 | 修复日期解析失败 | `lib/pages/data_backup_page.dart` | 支持 ISO 8601 + yyyy-MM-dd |
| 5 | 简化 Excel 列结构 | `lib/pages/data_backup_page.dart` | 去掉二级分类/ID/图标/排序列 |
| 6 | 月度总览颜色调整 | `lib/pages/home_page.dart` | 收入=红、支出=绿、结余正=红负=绿 |
| 7 | 月份切换功能 | `lib/pages/home_page.dart` | 点击选择年月 + 左右滑动切换 + 本月记录 |
| 8 | Slidable 左滑删除 | `lib/pages/home_page.dart` | 使用 `flutter_slidable`，无需确认对话框 |
| 9 | 分类图标选择 | `lib/pages/category_page.dart` | 添加/编辑时可选 17 个预设图标 |
| 10 | 分类长按拖动排序 | `lib/pages/category_page.dart` | `ReorderableDelayedDragStartListener` |
| 11 | 分类批量管理模式 | `lib/pages/category_page.dart` | 勾选批量删除 + 拖动排序 |
| 12 | 预设分类开放编辑删除 | `lib/pages/category_page.dart` | 移除 `isDefault == 1` 限制 |

### 遇到的问题

| # | 问题 | 根因 | 修复 |
|---|------|------|------|
| 1 | `excel.delete('Sheet1')` 不生效 | `excel` 包 v4.0.6 中 `delete` 有 bug | 改用 `excel.rename('Sheet1', '收支记录')` |
| 2 | 导入失败「未解析到有效记录」 | 日期格式为 ISO 8601，代码只支持 `yyyy-MM-dd HH:mm:ss` | `_parseDate()` 兼容两种格式 |
| 3 | `ReorderableListView.of()` 不存在 | API 不存在 | 改用 `ReorderableDelayedDragStartListener` |
| 4 | 分类管理页面崩溃 | `ReorderableDelayedDragStartListener` 手势冲突 + key 层级错误 | 回退为 `ReorderableDragStartListener` + 拖动图标 |

### 收尾调整

- [x] 去掉分类拖动排序（用户反馈不好看，不影响使用）

### 下一步

- [ ] 用户实机测试反馈处理
- [ ] 第三期 MVP：图表分析、自定义图标、付费功能

---

---

## 2026-06-10 — 收支分析图表

### 完成事项

| # | 事项 | 涉及文件 | 备注 |
|---|------|----------|------|
| 1 | 添加图表库 `fl_chart` | `pubspec.yaml` | v0.68.0，支持柱状图/饼图/折线图 |
| 2 | 收支分析图表页面 | `lib/pages/analysis_page.dart`（新建） | Tab 切换：趋势（近6月柱状图）/ 支出环形图 / 收入环形图 |
| 3 | 首页入口调整 | `lib/pages/home_page.dart` | 点击每月总览卡片 → 分析页；AppBar 新增日历图标 → 月份选择 |
| 4 | 新增数据库查询 | `lib/database/db_helper.dart` | `getMonthlySummary(months)`、`getDailySummary(year, month)` |
| 5 | MuMu 模拟器测试 | — | 成功运行，图表渲染正常 |
| 6 | 生成 Release APK | `PalmSugar-v1.0.0.apk` | 54MB，复制到项目根目录 |

### 遇到的问题

| # | 问题 | 根因 | 修复 |
|---|------|------|------|
| 1 | `fl_chart` 编译报错（类型不匹配） | `num` vs `double` 推断问题 | 显式 `.toDouble()` 转换 |
| 2 | `Color.shade300` 不存在 | 参数类型为 `Color`，但 `shadeN` 只在 `MaterialColor` 上 | 参数类型改为 `MaterialColor` |
| 3 | MuMu 模拟器未在 `flutter devices` 显示 | adb 未连接 | 用 MuMu 自带 adb `connect 127.0.0.1:7555` |

### 关键决策

- 支付宝/微信导入：**没有公开 API**，只能走「用户导出 CSV/Excel → 本 App 导入文件」的方式
- 用户自行研究 CSV 导出格式，下次继续对接

### 下一步

- [ ] 支付宝/微信 CSV 账单格式解析
- [ ] 理财交易管理（基金买卖、持仓概览）
- [ ] 用户真机测试反馈

---

## 2026-06-12 — CSV 导入导出 + 焦点修复 + 第三方导入搁置

### 完成事项

| # | 事项 | 涉及文件 | 备注 |
|---|------|----------|------|
| 1 | 修复 xlsx 导入 numFmtId 崩溃 | `lib/pages/data_backup_page.dart` | 捕获 `excel` 包已知 bug，给出友好提示，引导用户转 CSV |
| 2 | 新增 CSV 导入导出 | `lib/pages/data_backup_page.dart` + `pubspec.yaml` | 引入 `csv: ^6.0.0`，CSV 与 xlsx 共用同一套行解析逻辑 |
| 3 | 导出格式选择对话框 | `lib/pages/data_backup_page.dart` | 导出前弹窗选择 Excel (.xlsx) 或 CSV |
| 4 | 修复日期选择焦点回跳 | `lib/pages/add_record_page.dart:71` | `_selectDate()` 开头增加 `FocusScope.of(context).unfocus()` |
| 5 | 分析支付宝/微信导出格式 | `docs/Alipay.csv` + `docs/wechat.xlsx` | 确认分类体系不兼容、编码/头部差异大，决定搁置 |
| 6 | 生成 Release APK | `棕榈糖账本_20260611.apk` | 54MB，复制到项目根目录 |

### 遇到的问题

| # | 问题 | 根因 | 修复 |
|---|------|------|------|
| 1 | xlsx 导入崩溃 `numFmtId starts at 164 but found a value of 7` | `excel` 包严格断言自定义 numFmtId ≥ 164，WPS/Numbers 生成的文件不遵守 | 捕获异常给出中文提示，引导用户用 CSV 导入 |
| 2 | 日期选择后键盘弹出、焦点回跳金额框 | `showDatePicker` 关闭后原 FocusNode 重新获得焦点 | 弹出前调用 `FocusScope.of(context).unfocus()` |

### 关键决策

| # | 决策点 | 选项A | 选项B | 选择 | 理由 |
|---|--------|-------|-------|------|------|
| 1 | numFmtId bug 修复方式 | 换 `excel_wps` fork | 保留原包 + 增加 CSV 替代 | **保留原包 + CSV** | `excel_wps` 维护状态不明，CSV 更通用可靠 |
| 2 | 第三方导入（支付宝/微信/同花顺） | 立即开发 | 搁置 | **搁置** | 编码/分类映射/去重复杂度高，等人手充足后统一规划 |

### 下一步

- [ ] 用户真机测试反馈（日期焦点、CSV 导入导出）
- [ ] 第三期 MVP 规划（图表增强、自定义图标、付费功能）

---

## 备注

- 当前代码质量：`flutter analyze` 零错误
- 用户指定主色调 `#A3C1AD`（柔和薄荷绿），已替换
- App 名称：`棕榈糖账本`
- 第二期预留表结构已设计：`accounts`、`fund_holdings`、`fund_transactions`
- 踩坑记录已归档到 `docs/bugs/` 并同步到 Obsidian Vault
