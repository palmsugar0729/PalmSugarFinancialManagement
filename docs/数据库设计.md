# 数据库设计 — PalmSugar Financial Management

基于 `收支管理记录.xlsx` 数据结构及 MVP 分期设计。

---

## 技术选型

- **Flutter 插件**: `sqflite` + `path_provider`
- **数据库**: SQLite 3
- **版本管理**: 使用 sqflite 的 onCreate/onUpgrade 机制

---

## 表结构

### 1. categories（收支分类表）

```sql
CREATE TABLE categories (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  parent_id INTEGER DEFAULT 0,          -- 0 表示一级分类，否则为二级分类
  name TEXT NOT NULL,                    -- 分类名称
  type TEXT NOT NULL CHECK(type IN ('expense', 'income', 'transfer')), -- 收支类型
  icon_name TEXT,                        -- Flutter icon 名称（如 'restaurant'）
  sort_order INTEGER DEFAULT 0,          -- 排序权重
  is_default INTEGER DEFAULT 0,          -- 1=预设分类，0=用户自定义
  is_deleted INTEGER DEFAULT 0,          -- 软删除标记
  created_at INTEGER,                    -- 时间戳（毫秒）
  updated_at INTEGER
);
```

**说明**:
- `parent_id = 0` 表示一级分类（如"餐饮"、"交通"）
- `parent_id > 0` 表示二级分类（如"早餐"、"午餐"）
- 第一期 MVP 先支持一级分类，二级分类预留字段

**预设分类数据**（参考 Excel 分类表）：

| 类型 | 一级分类 |
|------|---------|
| expense | 餐饮、交通、购物、居住、娱乐、医疗、教育、通讯、人情、其他 |
| income | 工资、奖金、投资收益、兼职、红包、退款、其他 |
| transfer | 转账 |

---

### 2. transactions（收支记录表）

```sql
CREATE TABLE transactions (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  type TEXT NOT NULL CHECK(type IN ('expense', 'income', 'transfer')), -- 收支类型
  amount REAL NOT NULL,                  -- 金额（元，保留两位小数）
  category_id INTEGER NOT NULL,          -- 分类 ID
  sub_category_id INTEGER DEFAULT 0,     -- 二级分类 ID（预留）
  account_id INTEGER DEFAULT 0,          -- 账户 ID（预留，第二期）
  to_account_id INTEGER DEFAULT 0,       -- 目标账户 ID（转账时使用，预留）
  date INTEGER NOT NULL,                 -- 日期时间戳（毫秒，精确到天）
  note TEXT,                             -- 备注
  tag TEXT,                              -- 标签（逗号分隔，预留）
  is_deleted INTEGER DEFAULT 0,          -- 软删除标记
  created_at INTEGER,                    -- 创建时间戳（毫秒）
  updated_at INTEGER,
  FOREIGN KEY (category_id) REFERENCES categories(id)
);

CREATE INDEX idx_transactions_date ON transactions(date);
CREATE INDEX idx_transactions_category ON transactions(category_id);
CREATE INDEX idx_transactions_type ON transactions(type);
```

---

### 3. answers（答案之书题库）

```sql
CREATE TABLE answers (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  content TEXT NOT NULL,                 -- 答案内容
  category TEXT,                         -- 答案分类（如 "鼓励", "建议", "提醒"）
  is_active INTEGER DEFAULT 1            -- 是否启用
);
```

**预设答案数据**（第一期内置 20 条）：
- 鼓励类："相信自己，你正在正确的道路上。"、"每一个小进步都值得庆祝。"
- 建议类："现在可能是开始储蓄的好时机。"、"检查一下你的月度预算吧。"
- 提醒类："别忘了记录今天的开销。"、"理性消费，快乐生活。"

---

## 当前开发表（第三期 — 理财模块）

### 4. fund_holdings（基金持仓表）

```sql
CREATE TABLE fund_holdings (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  fund_name TEXT NOT NULL,               -- 基金名称
  fund_code TEXT,                        -- 基金代码（6位）
  fund_type TEXT,                        -- 基金类型（混合/股票/债券/货币/指数等）
  total_buy_amount REAL DEFAULT 0,       -- 累计买入总金额（不含手续费，用于移动加权平均）
  total_fee REAL DEFAULT 0,              -- 累计手续费
  total_shares REAL DEFAULT 0,           -- 累计买入总份额
  holding_shares REAL DEFAULT 0,         -- 当前持有份额
  cost_per_share REAL,                   -- 单位成本（移动加权平均法）
  current_nav REAL,                      -- 当前净值（手动录入或后续联网更新）
  holding_amount REAL,                   -- 持仓市值 = holding_shares × current_nav
  cost_amount REAL,                      -- 成本金额 = holding_shares × cost_per_share
  profit REAL,                           -- 收益 = holding_amount - cost_amount
  profit_rate REAL,                      -- 收益率 = profit / cost_amount
  is_deleted INTEGER DEFAULT 0,
  created_at INTEGER,
  updated_at INTEGER
);
```

> **移动加权平均法**：每次买入后重新计算 `cost_per_share = (原成本金额 + 本次买入金额) / (原份额 + 本次买入份额)`，卖出时不改变成本价。

### 5. fund_transactions（基金交易记录表）

```sql
CREATE TABLE fund_transactions (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  fund_id INTEGER NOT NULL,              -- 关联 fund_holdings
  type TEXT NOT NULL CHECK(type IN ('buy', 'sell', 'dividend')),
  date INTEGER NOT NULL,                 -- 交易日期（毫秒时间戳）
  amount REAL,                           -- 交易金额（买入=投入金额，卖出=到账金额，分红=分红金额）
  fee REAL DEFAULT 0,                    -- 手续费
  nav REAL,                              -- 净值（买入/卖出时有效，分红为 NULL）
  shares REAL,                           -- 份额（买入/卖出时有效，分红为 NULL）
  dividend_amount REAL,                  -- 分红金额（仅 type='dividend' 时有效）
  synced_transaction_id INTEGER DEFAULT 0, -- 关联的首页 transactions.id（自动同步用）
  note TEXT,
  created_at INTEGER,
  FOREIGN KEY (fund_id) REFERENCES fund_holdings(id)
);
```

> **关于手续费**：
> - 买入：`份额 = (amount - fee) / nav`，成本计算不含手续费
> - 卖出：`到账金额 = shares × nav - fee`
> - 分红：无手续费

### 6. accounts（账户表 — 预留）

（略，后续开发）

---

## 数据库版本演进

| 版本 | 内容 |
|------|------|
| v1 | 创建 categories、transactions、answers 表（第一期 MVP） |
| v2 | 创建 fund_holdings、fund_transactions 表（第三期 — 理财模块） |
| v3 | 创建 accounts 表等（后续） |

---

## 关键查询示例

### 当月收支汇总

```sql
SELECT 
  type,
  SUM(amount) as total
FROM transactions
WHERE 
  date >= ? AND date < ?  -- 当月时间范围
  AND is_deleted = 0
GROUP BY type;
```

### 分类占比统计

```sql
SELECT 
  c.name,
  c.type,
  SUM(t.amount) as total,
  ROUND(SUM(t.amount) * 100.0 / (SELECT SUM(amount) FROM transactions WHERE type = ? AND date >= ? AND date < ? AND is_deleted = 0), 2) as percentage
FROM transactions t
JOIN categories c ON t.category_id = c.id
WHERE 
  t.type = ?
  AND t.date >= ? AND t.date < ?
  AND t.is_deleted = 0
GROUP BY c.id
ORDER BY total DESC;
```

### 随机获取一条答案

```sql
SELECT content FROM answers 
WHERE is_active = 1 
ORDER BY RANDOM() 
LIMIT 1;
```
